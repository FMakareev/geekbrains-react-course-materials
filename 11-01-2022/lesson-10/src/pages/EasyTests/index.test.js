

function sum(a, b) {
  return a + b;
}

/**
 *
 * @return {string}
 * */
const concatName = (separate = ' ') => (...args) => {
  return args.join(separate)
}

describe('EasyTests', () => {

  test.skip('adds 1 + 2 to equal 3', () => {

  });

  it('без аргументов', () => {

  })

  it('слияние па параметров в строку через пробел', () => {

  })

  it('слияние па параметров в строку через заданный разделитель', () => {

  })


})
